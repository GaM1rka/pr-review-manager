# 🛠️ PR Reviewer Assignment Service

Микросервис для автоматического назначения ревьюеров на Pull Request в команде.

---

## 📖 Описание проекта

Сервис позволяет:
- создавать команды и управлять участниками
- назначать до двух ревьюеров на Pull Request
- переназначать ревьюеров при необходимости
- отслеживать PR, где пользователь выступает ревьювером

Используется PostgreSQL в качестве базы данных.

**Описание задачи:**  
Микросервис реализует систему автоматического распределения ревьюеров по Pull Request для команд.  

---

## ⚙️ Требования

- Docker >= 20.10
- Docker Compose >= 1.29
- Make
- Go 1.21+ (для локальной разработки)
- PostgreSQL (запускается в контейнере через docker-compose)

---

## 🚀 Быстрый старт

Собрать и запустить сервис:

make up

Эта команда выполнит:
- сборку Docker образов
- запуск сервиса и базы данных PostgreSQL
- применение всех миграций

Сервис будет доступен по адресу:  
http://localhost:8080

---

## 🏗️ Структура запуска

После успешного старта контейнеров:
- приложение готово к приему API-запросов
- база PostgreSQL доступна внутри сети Docker
- для тестирования API можно использовать Postman или curl

---

## 🔧 Переменные окружения

| Переменная         | Описание                        | Пример    |
|--------------------|---------------------------------|-----------|
| POSTGRES_USER      | Имя пользователя PostgreSQL      | myuser    |
| POSTGRES_PASSWORD  | Пароль для PostgreSQL            | secret    |
| POSTGRES_DB        | Название базы данных             | mydb      |
| POSTGRES_PORT      | Порт PostgreSQL                  | 5432      |
| APP_PORT           | Порт приложения                  | 8080      |

Все переменные можно задать в `.env` файле или в `docker-compose.override.yml`.

---

## 📡 Примеры API запросов

**Создать команду**
POST /team/add
Content-Type: application/json

{
"team_name": "backend",
"members": [
{"user_id": "u1", "username": "Alice", "is_active": true},
{"user_id": "u2", "username": "Bob", "is_active": true}
]
}


**Получить команду**

GET /team/get?team_name=backend


**Создать Pull Request**
POST /pullRequest/create
Content-Type: application/json

{
"pull_request_id": "pr-1001",
"pull_request_name": "Add search",
"author_id": "u1"
}


**Смерджить Pull Request**
POST /pullRequest/merge
Content-Type: application/json

{
"pull_request_id": "pr-1001"
}


---

## 🖥️ Информация для локальной разработки

1. Склонировать репозиторий:
    ```
    git clone <repo-url>
    cd <project-folder>
    ```
2. Поднять сервис и базу:
    ```
    make up
    ```
3. Тестировать API через Postman или curl.

**Postman Collection для тестирования:**
- `postman/my-api-collection.postman_collection.json`
- `postman/load_1k.csv`

Импортировать коллекцию → опционально добавить CSV → запустить через Runner.

---

## 📊 Результаты нагрузочного тестирования

Сервис был протестирован вручную с помощью собственного скрипта и инструментария для нагрузочного тестирования.

**Результаты:**

- Число итераций: 1000
- Длительность теста: 1 минута 26 секунд
- Среднее время отклика: 4 мс

Сервис стабильно выдержал заданную нагрузку и показал минимальное среднее время ответа в тестовых условиях.
